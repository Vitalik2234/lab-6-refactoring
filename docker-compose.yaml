version: '3.9'

services:
  app:
    build: .
    container_name: python_app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app

  tests:
    build: .
    container_name: python_tests
    command: pytest